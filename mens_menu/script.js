// 90's Kid Unisex Saloon — Men's Service Menu
// Currently no interactivity is required; this file is wired in
// (index.html already loads it) so you can add behaviour later
// without touching the HTML again, e.g.:
//
//   - highlight a section when tapped
//   - a "Book Now" button that opens WhatsApp/Call
//   - toggle to reveal the offer/member price columns instead of
//     hand-editing the HTML comments in index.html
//
// Example toggle you can uncomment once member pricing is live:
//
// document.querySelectorAll('.price-old, .price-member').forEach(el => {
//   el.style.display = 'inline';
// });

document.addEventListener('DOMContentLoaded', () => {
  // no-op for now
});
